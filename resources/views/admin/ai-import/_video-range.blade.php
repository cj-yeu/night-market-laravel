<details class="my-3"><summary>Advanced: video segment</summary><p class="small mt-2" id="{{ $rangeId }}-range-help">One video, up to 180 seconds per analysis. Default: first 120 seconds, or the video end if earlier. This is segment analysis, not full-video analysis. Open the source to verify its length.</p><div class="row g-2"><label class="col-sm-6" for="{{ $rangeId }}-start">Start (seconds)<input id="{{ $rangeId }}-start" name="video_start_seconds" type="number" min="0" max="43199" class="form-control" value="{{ old('video_start_seconds',0) }}" aria-describedby="{{ $rangeId }}-range-help"></label><label class="col-sm-6" for="{{ $rangeId }}-end">End (seconds)<input id="{{ $rangeId }}-end" name="video_end_seconds" type="number" min="1" max="43200" class="form-control" value="{{ old('video_end_seconds',120) }}" aria-describedby="{{ $rangeId }}-range-help"></label></div></details>
